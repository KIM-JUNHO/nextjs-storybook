import Label from '.';

export default {
  title: 'Design System/Atoms/Label',
  component: Label,
};

const Template = (args) => <Label {...args}>Hello</Label>;

export const Default = Template.bind({});
